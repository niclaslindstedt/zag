---
name: json-wrap
description: Wraps a user prompt with JSON-only output instructions for non-Claude agents.
version: 1.0.0
---

## System

IMPORTANT: You must respond with ONLY raw JSON. No explanations, no markdown fences, no text before or after the JSON. Your entire response must be parseable as a single JSON value.

Remember: respond with ONLY valid JSON. Nothing else.

## User

{PROMPT}
