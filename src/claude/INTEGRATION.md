# Claude JSON Output Parsing — Integration Notes

> **Note**: This is an internal developer document. The integration described here has been completed. See `src/claude/models.rs` for the Claude JSON models and `zag-lib/src/output.rs` for the unified `AgentOutput` format.

For the Claude JSON output format reference, see [README.md](README.md) in this directory.
